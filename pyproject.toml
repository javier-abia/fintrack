[project]
name = "fintrack"
version = "0.1.0"
description = "Financial Tracker"
license = { text = "MIT" }
readme = "README.md"
requires-python = "==3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/api",
]
