DATABASE_URL=sqlite+aiosqlite:///./data/fintrack.db

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-in-production

# Generate with: uv run python -c "from passlib.context import CryptContext; \
#   print(CryptContext(['bcrypt']).hash('yourpassword'))"
SINGLE_USER_PASSWORD_HASH=

DEBUG=false
